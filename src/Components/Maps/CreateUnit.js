import React from "react";
const CreateUnit = ({
  newUnitElement,
  setAddElement,
  pointsSquence,
}) => {
  return (
      <div>
       <button>
        Upload Zone
      </button>
    </div>
  );
};

export default CreateUnit;
